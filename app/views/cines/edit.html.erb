<div class="container is-fluid">
    <h1 class="title">Editing Cine</h1>
</div>

<%= render 'form', cine: @cine %>

<div class="container is-fluid mt-2">
    <div class="field is-grouped">
        <p class="control">
            <%= link_to '<i class="fas fa-arrow-left"></i> Back'.html_safe, cines_path, class: "button is-text" %>
        </p>
        
        <p class="control">
            <%= link_to '<i class="far fa-eye"></i> Show'.html_safe, @cine, class: "button is-text" %> 
        </p>
    </div>
</div>
